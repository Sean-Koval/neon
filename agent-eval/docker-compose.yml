version: "3.8"

services:
  # PostgreSQL database
  postgres:
    image: postgres:16-alpine
    container_name: agenteval-postgres
    environment:
      POSTGRES_USER: agenteval
      POSTGRES_PASSWORD: agenteval
      POSTGRES_DB: agenteval
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agenteval"]
      interval: 5s
      timeout: 5s
      retries: 5

  # MLflow tracking server
  mlflow:
    image: ghcr.io/mlflow/mlflow:v2.19.0
    container_name: agenteval-mlflow
    command: >
      mlflow server
      --backend-store-uri postgresql://agenteval:agenteval@postgres:5432/mlflow
      --default-artifact-root /mlflow/artifacts
      --host 0.0.0.0
      --port 5000
    ports:
      - "5000:5000"
    volumes:
      - mlflow_artifacts:/mlflow/artifacts
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      MLFLOW_TRACKING_URI: postgresql://agenteval:agenteval@postgres:5432/mlflow

  # AgentEval API
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: agenteval-api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://agenteval:agenteval@postgres:5432/agenteval
      MLFLOW_TRACKING_URI: http://mlflow:5000
      GOOGLE_CLOUD_PROJECT: ${GOOGLE_CLOUD_PROJECT:-}
      VERTEX_AI_LOCATION: ${VERTEX_AI_LOCATION:-us-central1}
    volumes:
      - ./api/src:/app/src:ro
    depends_on:
      postgres:
        condition: service_healthy
      mlflow:
        condition: service_started
    profiles:
      - full

  # AgentEval Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: agenteval-frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    volumes:
      - ./frontend/app:/app/app:ro
      - ./frontend/components:/app/components:ro
    depends_on:
      - api
    profiles:
      - full

volumes:
  postgres_data:
  mlflow_artifacts:
